def meow():
    _ = int(input("How many times do you want to meow? "))
    while i != 0: # alternatively use -- for _ in range(i)
        print("Meow.")
        i += 1 # += or -= shorthands to increment or decrement

meow()